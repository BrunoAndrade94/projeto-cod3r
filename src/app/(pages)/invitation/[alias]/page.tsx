export default async function PageInvitation(props: any) {
  // const params: any = use(props.params);
  // const a = params.alias;

  return (
    <div>
      <span>{props.params.alias}</span>
    </div>
  );
}
